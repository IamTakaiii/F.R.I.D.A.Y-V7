# Adapter — Obsidian

`brain.root` = vault absolute path. Filesystem tools only unless the user enables Obsidian MCP.

Layout SoT: `schema/vault.md`. Do not create `00 - Index.md` or a Threads folder.

Wiki links for in-vault hops. Number-prefixed **project** folders; feature **files** use unnumbered names (`intent.md`, `design.md`, `queue.md`).
