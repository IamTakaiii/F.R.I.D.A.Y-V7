# Adapter — Local FS

When `brain.backend: local`. Default fallback root: `~/.local/share/friday/`.

Same folder map as `schema/vault.md`. Filesystem tools only. No Obsidian MCP.

Create the fallback root only on the first approved durable write.
