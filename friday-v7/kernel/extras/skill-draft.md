# Friday v7 — Skill draft

Folder: `docs/skill-drafts/`. Never auto-register `/fr-*`.

```
success → memory skill_draft (am) → docs/skill-drafts/<slug>.md
              ├─ soft-load (read-only) on match
              └─ promote (explicit) → domain mode + version bump
```

Frontmatter: `type: skill-draft` · `slug` · `status` · `rev` · `evidence` · `promote_to` · `triggers[]`.  
Skip: one-off · already live · secrets.
