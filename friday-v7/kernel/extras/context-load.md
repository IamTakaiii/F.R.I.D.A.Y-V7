# Friday v7 — Context load (card / deep)

## Layers

| Layer | What | When |
|---|---|---|
| **Card** | Mode or standard `.md` | Always for that mode |
| **Deep** | Sibling `*.deep.md` | Trigger only |

## Deep triggers (any one)

- User: `ละเอียด` / `expand` / `full` / `deep load` / `โหลดเต็ม`
- Mode says load deep when scoring / locking / first time this session
- Dispute needs the full rubric
- Persisting a vault note that needs the full table

## Rules

1. Cold path unchanged: `runtime` → domain SKILL → **one mode card**.
2. Do not preload every `*.deep.md`.
3. If deep already in context → do not re-read.
4. Card must be enough to run the mode.
5. One deep file per heavy standard.

## Artifact reads

Separate from card/deep. Follow `schema/artifact.md`: card first, then named `parts[]`.
