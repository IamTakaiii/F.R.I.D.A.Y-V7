---
description: Build one work item test-first: confirm seams, then red → green per vertical slice.
---
Load `friday-v7/surfaces/fr-implement/SKILL.md`; select mode `tdd`. Continue with `$ARGUMENTS`.
