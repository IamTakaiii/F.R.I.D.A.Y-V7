# TickTick — init

First-run. Safe to rerun. Creates **zero** tasks.

Verify tools/auth + timezone. Resolve brain projects. Find or ask for registry (`type` note or configured path). Propose adopt/create mappings — never rename a list silently.

Default groups: Work Projects · Personal Projects · Areas & Routines. Default areas: Home · Health · Finance & Admin.

Preview all writes. Persist missing list IDs, timezone, `sync_version: 1`, `queue_limit: 5`. Re-read must not duplicate. End with a sync preview only — apply needs `/fr-ticktick` sync after separate approval.
