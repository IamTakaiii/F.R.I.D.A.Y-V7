# TickTick — setup

Maintain mappings. Do not rerun first-use ceremony.

Add / adopt / remap / archive a project list. Enable/disable sync. Queue limit 1–5. Change working hours / buffer / schedule prefs. Move Work ↔ Personal. Add an Area only after repeated uncategorized work.

Read registry + live IDs first. Preview registry and external changes together. Never remap by title when IDs disagree. Archive ≠ delete tasks or Vault sources.
