# TickTick — describe

Backfill Description only. No create / complete / schedule / sync / delete.

Scope: named list or today+overdue; active; skip children if parent in scope; skip already-full; apply ≤10 per approval.

Thin = empty / title-only / Goal-only / missing Goal·Context·DoD·Out of scope·Verify.

Evidence: Vault-linked → queue + Design; URL → that URL; Area → title/dates/checklist + `Assumed`. Never invent.

Preview then apply. CHECKLIST → `desc`; TEXT/NOTE → `content`. Do not change title/dates/status unless asked.
