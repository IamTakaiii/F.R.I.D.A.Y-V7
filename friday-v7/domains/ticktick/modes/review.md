# TickTick — review

Keep queues useful (1–5). Not a second backlog.

Per project: count · completed awaiting sync · blocked/conflict · next eligible. Propose refill as session bundles (consecutive same-feature work, 45–120m), not one task per Vault row. Skip IDs already covered.

After approval: create bundles; every covered source ID on exactly one task. First task gets the lowest `sortOrder`. Do not invent tasks or estimates.
