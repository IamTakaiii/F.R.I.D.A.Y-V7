# TickTick — manage

CRUD. Load `../standards/quality.md`. Write-gate before mutation.

List/filter/fetch. Create linked project task or TickTick-only Area task. Update title, Goal, dates, priority, reminders, recurrence, checklist. Move lists. Link/unlink without deleting. Complete/abandon — patch Vault terminal status only after TickTick succeeds. Delete only after explicit preview + confirm.

Create: session title + full Description. Prefer Estimate 45–120m. Tiny Vault rows → bundle or refuse a new task. Unique `sortOrder` (first in order = lowest). Title-only match only when exactly one task hits.
