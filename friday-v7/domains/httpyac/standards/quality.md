# httpyac quality

Load on write / from-design / run.

Customer-path order. State via `# @name`. Mutations: create → verify → cleanup. Happy + ≥1 failure. `??` status + body/header on critical steps. Secrets only as `{{var}}`. Endpoints from Design / OpenAPI / code — no invent.

Missing CLI → say `not-run`. Missing contract → refuse.
