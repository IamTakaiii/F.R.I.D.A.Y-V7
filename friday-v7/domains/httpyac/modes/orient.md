# httpyac — orient

Journey home: `{agent_root}/httpyac/<slug>/` if root exists; else offer once: init-agent **or** user path (e.g. `tests/http/`). Do not block other work.

Contract: cite ≥1 (Design API · OpenAPI/router · user paths). Journey one-liner + `.http` + env paths.

Route: write | from-design | run | note.
