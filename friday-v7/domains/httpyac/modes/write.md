# httpyac — write

Author `.http` + env scaffold. Load `../standards/quality.md`. Outline for approval first.

Journey steps · auth · happy + failure · cleanup. Regions `###` · `# @name` on ID/token steps · `??` asserts · `{{host}}` / `{{token}}` only.

Write `.env.example` (placeholders). If `.env` missing: empty keys, gitignored. Ensure ignore covers `.agent/**/.env` with `!.env.example`. Short `#` comments only — no commentary walls.
