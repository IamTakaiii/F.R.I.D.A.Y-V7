# Feature identity

Fail-closed. Prefer `absorb` / `extend` over a new feature when outcome/boundary already has an owner.

New folder only when the user-facing outcome is independently trackable and no existing Intent covers it. Duplicates → `/fr-sdlc` consolidate.
