# SDLC — test

Run the repo suite, optional bench/load, or propose tooling (init). One mode.

| Signal | Do |
|---|---|
| run / coverage | Repo tests. Report `kind: test` |
| load / benchmark | Bench vs Design bounds. Report `kind: perf` |
| ตั้งค่า mutation / k6 | Propose global CLI + recipe. Write `{agent_root}/` only until approved |

Report: `quality-report` (`schema/types/software/quality-report.md`) dated under `reports/`.

Load `../standards/test.md` and `../standards/perf.md` when that kind runs.

## Done

Command + result + report path, or skip / proposal path.
