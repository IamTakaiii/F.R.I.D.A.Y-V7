# Life — logistics

Bookings, visas, transport, lodging, packing hooks.

Deadline map. Open items: item · status · ref · due. Risks → `personal-risk` if heavy. No invented confirmation numbers.

Persist tasks as `personal-tasks` or a dated `note` in `90 - Log/` — pick the registered type that matches, via write-gate.
