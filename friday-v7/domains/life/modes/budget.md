# Life — budget

Currency + hard cap if any. Table: item · estimate · actual · note. Unknowns = `Assumed` / TBD — never fake quotes. Over cap → cut options (`counsel.md`).

Persist: `personal-budget` at `30 - Budget/<slug>.md`. Enable the module on the Brief if new. Evidence for web prices (`kernel/io/evidence.md`).
