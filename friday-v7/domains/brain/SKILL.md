# Domain: Brain

Second Brain ops. Adapter via `kernel/brain/port.md` only.

**Load:** `kernel/runtime.md` → this file → **one** `modes/*.md`.
Writes to vault: `kernel/io/write.md` + `schema/write-gate.md` — same type/path/template as every other domain.
Writes: `kernel/io/write.md`. Vault: `kernel/brain/port.md` + `schema/`.

## Modes
| Mode | File |
|---|---|
| capture | `modes/capture.md` |
| inbox | `modes/inbox.md` |
| link | `modes/link.md` |
| moc | `modes/moc.md` |
| health | `modes/health.md` |

Software projects use SDLC types. New personal projects → `/fr-life` init (`brief` + `project_kind: personal`).



## Done
Cite paths for durable work. Artifact caps apply when writing vault types.
