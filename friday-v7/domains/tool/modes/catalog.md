# Tool — catalog

Optional for S one-offs.

Prefer a one-liner in existing `scripts/README`, `tools/README`, or Makefile. Handoff Tools row only if `{agent_root}` exists (write-gate; never silent). Vault note optional — do not invent folder trees. No secrets. Update in place if the slug exists.
