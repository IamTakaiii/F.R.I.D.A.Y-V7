# Tool — orient

Pain · kind · reuse bar (one-off → ad-hoc, stop). Placement (neighbors first). Stack = repo (never invent). Tier S default; M if shared/multi-session/hard mutate; L → `/fr-design`. Slug + one-line purpose.

Route: shape | build (contract already locked) | harden | catalog.
