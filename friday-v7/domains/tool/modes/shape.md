# Tool — shape

Lock the contract before code. Load `../standards/quality.md`.

| Field | Content |
|---|---|
| Purpose | one line |
| Invoke | exact command sketch |
| Inputs | args, flags, stdin, env |
| Outputs | stdout, files, codes |
| Side effects | none / paths / network |
| Failures | when non-zero |
| Safety | dry-run? confirm? idempotent? |

Options ≥2 only when it matters. Recommend one; ask to lock. Tier S: chat (or header comment). Tier M: `README.md` / `TOOL.md` after write-gate. Then build.

No product-feature creep. No full Design.md for an S helper.
