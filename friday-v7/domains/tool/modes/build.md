# Tool — build

One tool / one slice against the locked contract. If the contract forked → shape.

Minimal implementation; match neighbors; no new deps without reason. CLI: `--help` or usage on bad args. Verify happy path; dry-run first if mutating. Print invoke + result or `not-run` + blocker.

Then harden if mutating/shared/secrets; else done or catalog. Do not silently re-open shape. Do not skip verify when runnable.
