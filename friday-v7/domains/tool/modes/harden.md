# Tool — harden

After build. Load `../standards/quality.md`.

Mutations: dry-run or confirm. Secrets: env + `.env.example` keys only. Paths: overridable. Idempotency: state truthfully. Failures: stderr + non-zero. Edges: empty / missing env / permission. Docs: purpose, invoke, env, dangers. Re-run the critical path.

Done when Must items pass. Catalog if shared / tier M / user wants a registry.
