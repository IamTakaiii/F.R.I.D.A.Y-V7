# Tool quality

Load on shape / build / harden. S default.

## Route

Repeat toil + clear I/O → this domain. One-off throwaway → ad-hoc script. Product → `/fr-design`. Authored `.http` → `/fr-httpyac`.

## Placement

Prefer repo `scripts/` or existing tool dir. `{agent_root}/tools/<slug>/` only if root exists. Slug: kebab.

## Must

Purpose + invoke one-liner. Exit non-zero on failure. Mutating: dry-run or confirm. Zero secrets. Stack matches neighbors. Verify ran or honest `not-run`.
